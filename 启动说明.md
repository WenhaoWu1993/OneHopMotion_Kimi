# One-Hop Motion 手机端启动说明

## 1. 查电脑局域网 IP

在 Terminal 执行：

```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

或简写：

```bash
ipconfig getifaddr en0
```

记下类似 `192.168.x.x` 的地址。

当前 IP 是：**`192.168.3.118`**（如果换过 WiFi 会变，每次启动前重新查一下）

---

## 2. 启动 HTTP 服务（基础访问）

在项目目录下执行：

```bash
cd /Users/wenhaowu/Desktop/OneHopMotion_Kimi
python3 -m http.server 8080 --bind 0.0.0.0
```

手机浏览器访问：

```
http://192.168.3.118:8080
```

> ⚠️ HTTP 模式下**没有陀螺仪效果**（现代浏览器限制），只能体验双击触发和卡片交互。

---

## 3. 启动 HTTPS 服务（带陀螺仪）

### 3.1 证书已生成过，直接启动

```bash
cd /Users/wenhaowu/Desktop/OneHopMotion_Kimi
python3 -c "
import ssl
from http.server import SimpleHTTPRequestHandler, ThreadingHTTPServer
port = 8443
server = ThreadingHTTPServer(('0.0.0.0', port), SimpleHTTPRequestHandler)
ctx = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
ctx.load_cert_chain('cert.pem', 'key.pem')
server.socket = ctx.wrap_socket(server.socket, server_side=True)
print(f'HTTPS: https://192.168.3.118:{port}')
server.serve_forever()
"
```

手机浏览器访问：

```
https://192.168.3.118:8443
```

第一次访问会提示"证书不安全"，点 **高级 → 继续前往** 即可。

### 3.2 如果证书文件丢失，重新生成

```bash
cd /Users/wenhaowu/Desktop/OneHopMotion_Kimi
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes -subj "/CN=localhost"
```

---

## 4. 不依赖同一 WiFi 的远程访问方案

如果手机和电脑不在同一个网络（比如手机用流量），用以下方案：

### 方案 A：ngrok（最稳定，需要注册）

1. 去 [ngrok.com](https://ngrok.com) 注册免费账号
2. 安装并配置：
   ```bash
   brew install ngrok
   ngrok config add-authtoken 你的token
   ```
3. 本地启动 HTTP 服务后，另开一个 Terminal：
   ```bash
   ngrok http 8080
   ```
4. 手机访问生成的 `https://xxx.ngrok-free.app` 链接

### 方案 B：cloudflared tunnel（免费，不用注册）

```bash
# 安装
brew install cloudflared

# 启动隧道（本地服务要先在 8080 端口跑起来）
cloudflared tunnel --url http://localhost:8080
```

手机访问生成的 `https://xxx.trycloudflare.com` 链接。

### 方案 C：部署到 Vercel / GitHub Pages（最推荐长期使用）

把项目推送到 GitHub，开启 GitHub Pages，或部署到 Vercel。获得永久 HTTPS 链接，不需要本地开服务器。

---

## 5. iPhone Safari 陀螺仪设置

iPhone Safari 对陀螺仪有**三个硬性要求**：

1. **必须通过 HTTPS 访问**
2. **必须在用户点击/触摸时请求权限**
3. **必须在 iOS 设置里开启"动作与方向访问"**

### 具体操作

**第 1 步：用 HTTPS 打开页面**
- 用 ngrok / cloudflared 获得 HTTPS 链接
- 或用自签名证书：`https://192.168.3.118:8443`

**第 2 步：检查 iOS 系统设置**
- 设置 → Safari → 隐私与安全 → **动作与方向访问** → 开启
- iOS 17+ 可能在：设置 → Safari → 高级 → **动作与方向访问**

**第 3 步：页面内授权**
- 打开页面后，**双击屏幕**触发碰一碰
- Safari 会弹出系统弹窗：**"是否允许此网站访问您的动作与方向？"**
- 点击 **"允许"**
- 倾斜手机，卡片就会跟着动了

> ⚠️ 如果弹窗没有出现，刷新页面后重新双击。代码里是在双击时请求的权限，必须在用户手势中才会弹窗。

---

## 6. 手机端操作

1. **清除浏览器缓存**（重要！代码更新后必须清缓存）
   - 华为浏览器：设置 → 清除缓存
   - Safari：设置 → Safari → 清除历史记录与网站数据
   - 或直接用**隐私/无痕模式**打开最省事
2. 打开上面的链接
3. **双击屏幕**触发"碰一碰"
4. 等卡片散落后，**倾斜手机**体验陀螺仪视差（HTTPS 模式下）
5. **单击卡片**进入聚焦态，**左右滑**切换，**上滑**发送

---

## 7. 已知限制

| 浏览器 | HTTP 陀螺仪 | HTTPS 陀螺仪 | 备注 |
|--------|------------|-------------|------|
| Chrome / Edge (Android) | ❌ | ✅ | 推荐 |
| Safari (iOS) | ❌ | ⚠️ 需授权 | 弹窗点"允许" |
| 华为浏览器 | ❌ | ❌ | 默认禁用陀螺仪事件，建议换 Chrome |
| 微信内置浏览器 | ❌ | ❌ | 不支持 |

如果陀螺仪不可用，页面左下角会显示红色 `IMU: unavailable`。

---

## 8. 停止服务

### 如果有 Terminal 窗口
在运行服务器的窗口按 `Ctrl + C`。

### 如果找不到 Terminal 窗口
```bash
# 查进程号
lsof -i :8080 -i :8443

# 杀掉进程（把查到的 PID 替换进去）
kill 进程号

# 或一键杀掉所有占用的 Python 进程
kill $(lsof -t -i :8080 -i :8443)
```
